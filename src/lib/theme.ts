
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#d32f2f', // Red
    },
    secondary: {
      main: '#ffa000', // Amber
    },
  },
});

export default theme;
